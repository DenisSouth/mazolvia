#include <MsTimer2.h>

volatile unsigned long   oldTime = micros();
volatile unsigned long  synch_oldTime = micros();
volatile unsigned long  currentTime = micros();
volatile unsigned long  synch_currentTime = micros();

const int datalen = 18; //18
volatile  unsigned long data[datalen] = {};
volatile int counter = 0;


volatile  unsigned long synch_data[datalen] = {};
volatile int synch_counter = 0;


//есть ли что то кроме нулей?
boolean isnone(unsigned long  thedata[datalen]) {
  noInterrupts(); //отключить прерывания
  for (int i = 0; i < datalen; i++) {
    if (thedata[i] > 0) {
        interrupts(); //включить прерывания
      return true;
    }
  }
    interrupts(); //включить прерывания
  return false;
  
}


//unsigned long crop_array(unsigned long  thedata[datalen],int thestart, int thecount) {
//  noInterrupts(); //отключить прерывания
//
//  unsigned long feed[thecount-thestart] = {};
//  int a = 0;
//  for (int i = thestart; i < thecount; i++) {
//     feed[a] = thedata[i];
//     a = a + 1;
//    }
//  
//    interrupts(); //включить прерывания
//  return feed;
//  
//}


void by_timer() {
  noInterrupts(); //отключить прерывания
  
  if (isnone(synch_data)) {


    
    
    for (int i = 0; i < datalen; i++) {
      Serial.print(synch_data[i]);
    }
    
    Serial.print(" -  ");
    
    int index = 0;
    uint8_t acc = 0;
    
    while (index < datalen) {
    acc = 0;
    for (int i=0; i<8; i++)
    {
      acc = acc << 1;
      if((synch_data[index + i]) == 1 )
      { acc |= 1;}
    }
    
    Serial.print(acc,HEX);

    index = index + 9; //8
}
    
    Serial.println();
  }
  memset(synch_data, 0, sizeof(synch_data)); //очистить вывод
  synch_counter = 0; //очистить счетчик
  
  interrupts(); //включить прерывания
}


void by_synch_interrupt() {
   noInterrupts(); //отключить прерывания
  if (synch_counter < datalen) {
    synch_data[synch_counter] = digitalRead(2);
    synch_counter = synch_counter + 1;
    interrupts(); //включить прерывания
    MsTimer2::start(); //если после последнего вызова прошло н секунд - вызовится by_timer()
  }
}


void setup() {
  Serial.begin(115200);
  MsTimer2::set(500, by_timer);
  MsTimer2::start();
  pinMode(2, INPUT_PULLUP);
  
  attachInterrupt(1, by_synch_interrupt, RISING); //CHANGE FALLING **RISING
}

void loop() {
//  Serial.print("Time: ");
//  Serial.println(micros());
//  delay(1000);
}
